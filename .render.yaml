services:
  - type: web
    name: cap-online
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash -lc "python manage.py migrate && gunicorn cap_online.wsgi:application --bind 0.0.0.0:$PORT"
